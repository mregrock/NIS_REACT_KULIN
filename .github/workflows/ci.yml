name: CI

on:
  push:
    paths:
      - 'HW1/**'
      - '.github/workflows/ci.yml'
  pull_request:
    paths:
      - 'HW1/**'
      - '.github/workflows/ci.yml'

defaults:
  run:
    working-directory: HW1

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: HW1/package-lock.json
      - run: npm ci
      - run: npm run lint
      - run: npm run format:check
      - run: npm run test:run
      - run: npm run build
